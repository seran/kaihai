<div id="toaster" class="fixed top-4 right-4 z-50 flex flex-col gap-2 w-full max-w-sm pointer-events-none">
  <% flash.each do |type, message| %>
    <%= render "shared/ui/toast", variant: flash_variant(type), message: message %>
  <% end %>
</div>